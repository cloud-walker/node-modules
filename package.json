{
  "name": "root",
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write \"**/*.{js,json,yaml,ts}\""
  },
  "devDependencies": {
    "husky": "^4.2.1",
    "lerna": "^3.20.1",
    "lint-staged": "^10.0.7",
    "prettier": "^2.0.5"
  }
}
