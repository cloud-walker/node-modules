{
  "name": "root",
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write \"**/*.{js,json,yaml,ts}\""
  },
  "devDependencies": {
    "@cloudwalker/prettier-config": "^0.1.1",
    "husky": "^4.2.1",
    "lerna": "^3.20.1",
    "lint-staged": "^10.0.7",
    "prettier": "^1.19.1"
  }
}
